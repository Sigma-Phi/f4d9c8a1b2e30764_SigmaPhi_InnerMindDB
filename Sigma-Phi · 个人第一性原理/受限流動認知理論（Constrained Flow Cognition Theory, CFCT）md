# 📌 理论名称

**受限流动认知动力学理论（Constrained Flow Cognitive Dynamics, CFCD）**

---

## 1. 形式系统生成（Formal System Construction）

### 中文

定义认知状态、边界、生成与回返机制：

S_t ∈ B  
G: S_t → {S_t(1), ..., S_t(n)}  
V: {S_t(i)} → S_t*  

S_{t+1} = V(G(S_t)) ∩ B  

其中：

- S_t：认知状态  
- B：语义/认知边界（constraint set）  
- G：生成算子（divergence operator）  
- V：回返算子（convergence operator）

### English

Define a constrained cognitive dynamical system where generative expansion and evaluative contraction operate within a bounded semantic space.

---

## 2. 關鍵量生成（Key Quantities）

### 中文

F_t = Var(S_t) / Var(C(S_t))  

D_t = E[||S_{t+1} - S_t||²]  

R_t = ||V(G(S_t)) - G(S_t)||  

B_t = Vol(B)  

κ_t = ||G|| / ||V||

### English

- F_t: flow efficiency (cognitive fluidity index)  
- D_t: drift magnitude  
- R_t: return correction strength  
- B_t: boundary capacity  
- κ_t: generation–reversion ratio  

---

## 3. 動態方程（Dynamics Equation）

### 中文

S_{t+1} = S_t + αG(S_t) - βV(G(S_t)) + γΠ_B(S_t)

其中 Π_B 表示边界投影。

### English

Cognitive evolution is driven by generative expansion, corrective convergence, and boundary projection.

---

## 4. 相變結構（Phase Structure）

| Phase | Condition | Behavior | Regime |
|------|----------|----------|--------|
| Over-generative | κ_t > 1+δ | F_t ↑, drift ↑ | semantic chaos |
| Balanced flow | κ_t ≈ 1 | stable F_t | optimal cognition |
| Over-corrective | κ_t < 1-δ | F_t ↓ | rigid collapse |

---

## 5. 主定理（Main Theorem）

### 中文

存在临界参数 κ_c，使得：

κ → κ_c ⇒ F_t → F*

max F_t occurs when generation and correction are balanced within B

### English

Cognitive efficiency is maximized at the critical balance between generative and corrective dynamics under bounded constraints.

---

## 6. Lyapunov 稳定性（Stability）

### 中文

V(S_t) = -∫ p(S) log p(S) dS + λ||S_t - Π_B(S_t)||²  

dV/dt ≤ -η ||V(G(S_t)) - S_t||² + ξ ||G(S_t)||

### English

System stability is governed by entropy reduction under boundary-constrained dynamics.

---

## 7. 实验验证（Experimental Protocol）

### 中文

1. 建立语义状态模型（embedding / latent space）  
2. 定义边界 B（topic constraint）  
3. 实现生成算子 G（sampling / diffusion）  
4. 实现回返算子 V（ranking / critic）  
5. 扫描 κ = ||G|| / ||V||  
6. 测量 F_t, D_t, R_t  
7. 找到临界 κ_c  

### English

Detect phase transitions by sweeping generation–correction ratio and measuring system observables.

---

## 8. 可证伪预测（Falsifiable Predictions）

1. 存在唯一 κ_c 使 F_t 最大化  
2. G ≫ V → entropy increases  
3. V ≫ G → system collapses to fixed point  
4. 边界收缩 → 可达状态指数下降  
5. 边界过宽 → semantic drift不可控  

---

## 9. 核心洞见（Core Insight）

### 中文

认知流动不是自由扩散，而是在边界约束下，由生成与回返构成的动态系统。

### English

Cognition is not free flow, but a bounded system where intelligence emerges from balance between generation and correction.
