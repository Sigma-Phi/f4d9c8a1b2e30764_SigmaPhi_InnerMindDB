# 📌 理論名稱（Theory Name）

理論名稱：受限流動認知動力學理論（Constrained Flow Cognitive Dynamics, CFCD）

---

## 1. 形式系統生成（Formal System Construction）

### 中文  
定義認知狀態、邊界、生成與回返機制：

\[
S_t \in \mathcal{B}
\]
\[
G: S_t \rightarrow \{S_t^{(1)}, ..., S_t^{(n)}\}
\]
\[
V: \{S_t^{(i)}\} \rightarrow S_t^*
\]
\[
S_{t+1} = V(G(S_t)) \cap \mathcal{B}
\]

其中：

- \(S_t\)：認知狀態  
- \(\mathcal{B}\)：語義/認知邊界（constraint set）  
- \(G\)：生成算子（divergence operator）  
- \(V\)：回返算子（convergence operator）  

### English  
Define a constrained cognitive dynamical system where generative expansion and evaluative contraction operate within a bounded semantic space.

---

## 2. 關鍵量生成（Key Quantities）

### 中文（數學定義）

\[
F_t = \frac{\mathrm{Var}(S_t)}{\mathrm{Var}(C(S_t))}
\]

\[
D_t = \mathbb{E}[\|S_{t+1} - S_t\|^2]
\]

\[
R_t = \|V(G(S_t)) - G(S_t)\|
\]

\[
B_t = \mathrm{Vol}(\mathcal{B})
\]

\[
\kappa_t = \frac{\|G\|}{\|V\|}
\]

### English（解釋）

- **F_t**: flow efficiency (cognitive fluidity index)  
- **D_t**: drift magnitude  
- **R_t**: return correction strength  
- **B_t**: boundary capacity  
- **κ_t**: generation–reversion ratio  

---

## 3. 動態方程（Dynamics Equation）

### 中文

\[
S_{t+1} = S_t + \alpha G(S_t) - \beta V(G(S_t)) + \gamma \Pi_{\mathcal{B}}(S_t)
\]

其中 \(\Pi_{\mathcal{B}}\) 為邊界投影算子。

### English  
Cognitive evolution is governed by a competition between generative expansion, corrective convergence, and boundary projection.

---

## 4. 相變結構（Phase Structure）

| Phase | Condition | Behavior | System Regime |
|------|----------|----------|----------------|
| Over-generative | κ_t > 1+δ | F_t ↑, drift ↑ | semantic chaos |
| Balanced flow | κ_t ≈ 1 | stable F_t | optimal cognition |
| Over-corrective | κ_t < 1−δ | F_t ↓ | rigid collapse |

---

## 5. 主定理（Main Theorem）

### 中文  
存在臨界比例 κ_c，使得：

\[
\kappa \rightarrow \kappa_c \Rightarrow F_t \rightarrow F^*
\]

\[
\max F_t \;\; \text{occurs when generation and correction are balanced within boundary } \mathcal{B}
\]

### English  
Cognitive efficiency is maximized at the critical balance between generative and corrective dynamics under bounded semantic constraints.

---

## 6. Lyapunov 穩定性（Stability）

### 中文  

定義系統熵型 Lyapunov 函數：

\[
V(S_t) = -\int p(S)\log p(S)\, dS + \lambda \|S_t - \Pi_{\mathcal{B}}(S_t)\|^2
\]

則：

\[
\frac{dV}{dt} \le -\eta \|V(G(S_t)) - S_t\|^2 + \xi \|G(S_t)\|
\]

### English  
System stability is governed by entropy reduction under boundary-constrained correction dynamics.

---

## 7. 實驗驗證（Experimental Protocol）

### 中文

1. 建立語義狀態模型（LLM embedding / latent space）  
2. 定義邊界 \(\mathcal{B}\)（topic constraint / policy space）  
3. 實作生成算子 \(G\)（sampling / diffusion / decoding temperature）  
4. 實作回返算子 \(V\)（ranking / critic / reward model）  
5. 掃描 κ = ||G|| / ||V||  
6. 測量 F_t、D_t、R_t  
7. 找出臨界 κ_c  

### English  
Detect cognitive phase transition by varying generation–correction ratio and measuring flow efficiency.

---

## 8. 可證偽預測（Falsifiable Predictions）

### 中文

1. 存在唯一 κ_c 使 F_t 最大化  
2. G ≫ V 時語義熵單調上升  
3. V ≫ G 時系統進入低變異固定點  
4. 邊界收縮導致可達狀態集合指數下降  
5. 過寬邊界導致語義不可辨識漂移  

---

## 9. 核心洞見（Core Insight）

### 中文  
認知流動不是自由擴散，而是在邊界約束下，由生成與回返構成的動態呼吸系統。

### English  
Cognition is not free flow, but a bounded dynamic system where intelligence emerges from the balance between generation and correction within constraints.
